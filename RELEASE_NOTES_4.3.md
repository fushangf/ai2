# 4.3 多页面产品化升级

- 新增产品落地页 `/`。
- 登录、注册完全分离，注册使用独立 `/register` 页面。
- 登录页提供“用户登录 / 管理员登录”选项卡。
- 新增管理员独立入口 `/admin/login`。
- 用户工作台改为 `/workspace`，保留 `/app` 兼容跳转。
- 管理后台改为侧边栏结构，包含总览、用户管理、使用统计和系统状态。
- 新增角色隔离接口 `/api/login/user` 与 `/api/login/admin`。
- 登录支持用户名或邮箱。
- MySQL 宿主机端口继续使用 3307。
